// #include <game.h>

// Game::Game(const char *title, int x, int y, int w, int h, Uint32 flag) {
//     SDL_Init(SDL_INIT_EVERYTHING);

//     window = SDL_CreateWindow(title, x, y, w, h, flag);
//     renderer = SDL_CreateRenderer(window, -1, SDL_RENDERER_ACCELERATED);
// }
